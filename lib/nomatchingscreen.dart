import 'package:flutter/material.dart';

class Nomatchingscreen extends StatelessWidget {
  const Nomatchingscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.white,
      ),
    );
  }
}
